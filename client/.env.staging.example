# Staging / Android build config for the Vite frontend.
#
# Copy this file to `.env.staging` when building or testing against the Northflank staging environment.
# Note that in CI (.github/workflows/deploy-play-staging.yml), VITE_API_URL is injected directly.

VITE_API_URL=https://site--leyble-hub--tkm4pp6r2kky.code.run
